
<div class="current-group" id="<%= @current_group.id %>">
  <h3>
    <%= "#{@current_group.this_turn?}".html_safe %>
    <%= "#{@current_group.name}: " %>
    <span id="update_score<%= @current_group.id %>" class="scores">
      <%= render partial: '/teacher_gameplay/scores' %>
    </span>
  </h3>
  <%= "It is currently this group's turn." if @current_group.group_turn?%><br>
  <button id="send-message" class="mdl-button mdl-js-button mdl-button--raised mdl-button--colored button-margin">
    Send Message
  </button>
</div>

<div class="supplies">
  <h5>Supplies
    <button type="button" class="add-button" id="addSupplies">
      <i class="fa fa-plus"></i> ADD
    </button>
  </h5>
  <ul>
    <% if @current_group.supplies.length == 0 %>
      <li>
        No Supplies Added
      </li>
    <% else %>
      <% @current_group.supplies.each do |s| %>
        <li><div id="s<%= s.id %>"><%= render partial: '/supplies/show', locals: { s: s } %></div></li>
      <% end %>
    <% end %>
  </ul>
</div>


<div id="notes<%= @current_group.id %>">
  <%= render partial: '/notes/index', locals: { group: @current_group } %>
</div>


<div id="send-message-container">
  <%= render partial: '/teacher_gameplay/send_message', locals: { group: @current_group } %>
</div>
