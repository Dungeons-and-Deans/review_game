<%= simple_form_for @supply, url: 'assign_supply' do |f| %>
  <%= f.input :name %>
  <%= f.input :amount %>
  <%= f.association :group, label_method: :name %>

  <%= f.button :submit, "Assign" %>
<% end %>
