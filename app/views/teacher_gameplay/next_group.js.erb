$("#next-turn").html("<%= j(render partial: 'next_turn', locals: { game_session: @game_session}) %>");
$('#current-group').html("<%= j(render partial: 'current_group', locals: { current_group: @current_group}) %>");
