<%= simple_form_for @group, url: "/teacher_gameplay/#{@game_session.id}/update_score/#{@group.id}", remote: true do |f| %>

  <%= f.input :score, label: false %>

  <%= f.button :submit, "Update" %>

<% end %>
