<div>
  <h3>Scores</h3>
  <% @game_session.groups.each do |group| %>
    <div><%= group.name %> <%= group.score %></div>
  <% end %>
</div>

<div>
  <h3>Current Group</h3>
  <div><%= @current_group.name %></div>
</div>
