<% if @current_group %>
  <h5>Notes</h5>
  <ul>
    <% @current_group.notes.each do |n| %>
      <li><%= n.content %></li>
    <% end %>
    <li>
      <div>
        <%= render partial: '/notes/form', locals: { id: @current_group.id, note: Note.new } %>
      </div>
    </li>
  </ul>
<% end %>
