<%= simple_form_for @student, html: { class: "col-md-4 col-md-offset-4" } do |f| %>
  <%= f.error_notification %>

  <%= f.input :first_name %>
  <%= f.input :last_name %>

  <%= f.hidden_field :teacher_id %>

  <%= f.button :submit, class: "col-md-4 col-md-offset-4" %>

<% end %>
