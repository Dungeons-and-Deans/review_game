<%= simple_form_for @student do |f| %>
  <%= f.error_notification %>

  <%= f.input :first_name %>
  <%= f.input :last_name %>

  <%= f.hidden_field :teacher_id %>

  <%= f.button :submit %>

<% end %>
