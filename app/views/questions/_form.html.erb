<div id="new-question-form" class="form-inline">
  <%= f.simple_fields_for :questions, @category.questions.build, remote: true do |ff| %>

    <%= ff.input :content, label: 'Question' %>
    <%= ff.input :difficulty_level, label: 'Difficulty' %>

  <% end %>
</div>
