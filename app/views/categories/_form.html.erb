<%= simple_form_for @category do |f| %>
  <%= f.error_notification %>

  <%= f.input :name %>
  <%= f.hidden_field :teacher_id %>

  <h3>Questions</h3>

  <div><%= render partial: 'questions_form', locals: {f: f} %></div>

  <%= f.button :submit %>

<% end %>
