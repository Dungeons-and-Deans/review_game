<h3><%= @category.name %></h3>

<ul>
  <% @questions.each do |question| %>
    <li><%= question.content %></li>
  <% end %>
</ul>

<div class="links">
  <%= link_to 'Edit/Add Question', edit_category_path(@category) %>
  <%= link_to 'New Game Scenario', new_game_path %>
</div>
