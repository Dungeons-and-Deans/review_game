<%= simple_form_for @game, html: { class: "col-md-4 col-md-offset-4" } do |f| %>
  <%= f.error_notification %>

  <%= f.input :name %>
  <%= f.association :icon %>
  <%= f.input :movement %>

  <%= f.hidden_field :teacher_id %>

  <%= f.button :submit %>

<% end %>
