<%= simple_form_for @game do |f| %>
  <%= f.error_notification %>

  <%= f.input :name %>
  <%= f.association :icon %>
  <%= f.input :movement %>

  <%= f.hidden_field :teacher_id %>

  <%= f.button :submit %>

<% end %>
