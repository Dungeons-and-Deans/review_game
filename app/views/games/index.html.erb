<h1>Listing Games</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Player Icon</th>
      <th>Movement Allowed</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @games.each do |game| %>
      <tr>
        <td><%= game.name %></td>
        <td><i class="<%= game.student_icon %> fa-2x"></i></td>
        <td><%= game.movement %></td>
        <td><%= link_to 'Show', game %></td>
        <td><%= link_to 'Edit', edit_game_path(game) %></td>
        <td><%= link_to 'Remove', game, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Game Scenario', new_game_path %>
<%= link_to 'Home', root_path %>
<%= link_to 'Questions', categories_path %>
<%= link_to 'Roster', students_path %>
<%= link_to 'Start New Game', '/game_sessions/new' %>
