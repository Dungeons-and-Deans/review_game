<div class="mdl-cell mdl-cell--12-col">
  <h3>Game Session Name: <%= @game_session.name %></h3>
  <ul>
    <li>Each team will sign in during the game using the passwords below.</li>
    <li>The submit button below the teams will assign the students to the created groups.</li>
  </ul>
</div>

<div class="mdl-cell mdl-cell--2-col mdl-shadow--4dp class-list">
  <div><h6>Drag and drop students to assign to a team.</h6></div>
  <ul class="groups-students" id="0">
    <% @students.each do |student| %>
      <li class="draggables" id="<%= student.id %>"><%= student.full_name %></li>
    <% end %>
  </ul>
</div>

<div class="mdl-cell mdl-cell--10-col">
  <div id="new-group-form-container">
    <%= render partial: 'new_group_form' %>
  </div>
</div>
<div id="footnotes_holder"></div>
